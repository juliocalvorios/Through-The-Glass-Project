import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Providers } from './providers'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Through the Glass | Live Weather Experience',
  description: 'A living window that breathes with real-time weather. Watch the rain fall, snow accumulate, and aurora dance.',
  keywords: ['weather', 'interactive', 'aurora', 'rain', 'snow', 'react', 'nextjs'],
  icons: {
    icon: '/favicon.svg',
  },
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  )
}
